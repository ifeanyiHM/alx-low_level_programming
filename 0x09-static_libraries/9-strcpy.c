#include "main.h"
#include <string.h>

/**
* _strcpy - A function
* @dest: parameter
* @src: parameter
* Return: return empty
*/

char *_strcpy(char *dest, char *src)
{
	return (strcpy(dest, src));
}
