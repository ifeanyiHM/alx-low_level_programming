#include "main.h"
#include <string.h>

/**
* _memset - A function
* @s: parameter
* @b: parameter
* @n: parameter
* Return: empty
*/

char *_memset(char *s, char b, unsigned int n)
{
	return (memset(s, b, n));
}
